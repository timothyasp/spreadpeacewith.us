#main
  - @profiles.each do |profile| 
    .profile
      %img(src="#{profile.profileImgPath}")
      .clear
      .infoWrap
        %h2= profile.name
        %p= profile.description
        %br
  / DON'T REMOVE THIS FUCKING COMMENT
#sidebar
  #counter
    %p Hungry Children Fed
    %h2= @counter
  #leaderboard
    %h2 Leaderboard
    %ul
      - @profiles.each do |profile| 
        %li 
          %img(src="#{profile.profileImgPath}")
          .sideTextWrap
            %h3= profile.name
            %p <strong>#{profile.beerbong.childrenFed}</strong> Children Fed
            <p><a href="#">view profile</a></p>
      / DON'T REMOVE THIS FUCKING COMMENT